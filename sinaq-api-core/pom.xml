<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>uz.sinaq</groupId>
    <artifactId>sinaq-api-parent</artifactId>
    <version>1.0.0</version>
  </parent>

  <artifactId>sinaq-api-core</artifactId>
  <name>Sinaq API Core</name>
  <description>
    Core of the Sinaq API automation framework: fluent DSL, HTTP abstraction (SPI),
    request/response models, assertions, configuration, events.
    ARCHITECTURE RULE: this module must never depend on RestAssured, TestNG or JUnit
    at compile/runtime scope. Enforced by maven-enforcer bannedDependencies below.
  </description>

  <dependencies>
    <!-- Phase 1: no published (compile-scope) dependencies. -->
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <executions>
          <execution>
            <id>enforce-core-purity</id>
            <goals><goal>enforce</goal></goals>
            <configuration>
              <rules>
                <bannedDependencies>
                  <message>
                    ARCHITECTURE VIOLATION: sinaq-api-core must not depend on
                    RestAssured, TestNG or JUnit at compile/runtime/provided scope.
                    Test-framework and engine integrations live in adapter modules.
                  </message>
                  <excludes>
                    <exclude>io.rest-assured:*:*:*:compile</exclude>
                    <exclude>io.rest-assured:*:*:*:runtime</exclude>
                    <exclude>io.rest-assured:*:*:*:provided</exclude>
                    <exclude>org.testng:*:*:*:compile</exclude>
                    <exclude>org.testng:*:*:*:runtime</exclude>
                    <exclude>org.testng:*:*:*:provided</exclude>
                    <exclude>org.junit.jupiter:*:*:*:compile</exclude>
                    <exclude>org.junit.jupiter:*:*:*:runtime</exclude>
                    <exclude>org.junit.jupiter:*:*:*:provided</exclude>
                  </excludes>
                  <searchTransitive>true</searchTransitive>
                </bannedDependencies>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.jacoco</groupId>
        <artifactId>jacoco-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>prepare-agent</id>
            <goals><goal>prepare-agent</goal></goals>
          </execution>
          <execution>
            <id>report</id>
            <phase>verify</phase>
            <goals><goal>report</goal></goals>
          </execution>
          <execution>
            <id>coverage-gate</id>
            <phase>verify</phase>
            <goals><goal>check</goal></goals>
            <configuration>
              <rules>
                <rule>
                  <element>BUNDLE</element>
                  <limits>
                    <limit><counter>LINE</counter><value>COVEREDRATIO</value><minimum>0.80</minimum></limit>
                    <limit><counter>BRANCH</counter><value>COVEREDRATIO</value><minimum>0.70</minimum></limit>
                  </limits>
                </rule>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
